{
  "name": "employee-calendar",
  "version": "1.0.0",
  "description": "Employee Calendar Application",
  "main": "index.js",
  "scripts": {
    "start": "ui5 serve --config=ui5.yaml --open index.html",
    "build": "ui5 build --config=ui5.yaml --clean-dest --dest dist",
    "lint": "eslint webapp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "@sap/ux-ui5-tooling": "^1.17.4",
    "@ui5/cli": "^3.5.0",
    "eslint": "^8.45.0",
    "ui5-middleware-livereload": "^0.8.4",
    "ui5-middleware-simpleproxy": "^0.9.0"
  }
}
